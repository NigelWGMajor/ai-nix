interface:
  display_name: "GAP Hierarchical Review"
  short_description: "Recursive COP review for related branches"
  default_prompt: "Use $gap to review this branch, DAC hierarchy, or Pro reconstruction hierarchy."
